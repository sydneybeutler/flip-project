import React from 'react';
import { Container, Typography } from '@mui/material';

const Study = () => {
    return (
        <Container>
            <Typography variant="h4">This is the Study page.</Typography>
            {/* Add your component's content here */}
        </Container>
    );
};

export default Study;
